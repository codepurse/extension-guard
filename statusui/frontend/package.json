{
  "name": "extension-guard-status-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "Static frontend for the Ward status window (no build step)",
  "scripts": {
    "build": "node -e \"process.exit(0)\""
  }
}
